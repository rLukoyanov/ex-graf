apiVersion: v1
kind: Service
metadata:
  name: app
  namespace: influx
  labels:
    app: influx-app
spec:
  selector:
    app: influx-app
  ports:
    - port: 8080
      targetPort: http
      name: http
  type: ClusterIP
