apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: influx
  labels:
    app: grafana
spec:
  selector:
    app: grafana
  ports:
    - port: 3000
      targetPort: http
      name: http
  type: ClusterIP
